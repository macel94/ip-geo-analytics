# ip-geo-analytics
Sample full-stack TypeScript app that captures HTTP request metadata  (IP, User-Agent, referrer, GeoIP) on every page visit and displays  aggregated analytics + interactive map in a protected dashboard.
