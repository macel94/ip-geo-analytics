# Technical talk slides

This folder was initialized with the official Slidev scaffolder:

```bash
npm init slidev@latest docs
```

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The deck entrypoint is `slides.md`, and the talk is split into files under `pages/` using Slidev's `src:` imports.
